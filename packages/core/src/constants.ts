import type { ZrenderHelperOptions } from "./type";

export const OPTIONS: ZrenderHelperOptions = {
  inspection: true,
  rectProps: {
    style: {
      fill: "rgba(40,196,246,0.6)",
    },
    ignore: true,
    zlevel: Number.MAX_SAFE_INTEGER,
  },
};
